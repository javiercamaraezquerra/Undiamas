<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ───────── PERMISOS ───────── -->
    <!-- Red (anuncios) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Alarmas exactas (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- Notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- ───────── APLICACIÓN ───────── -->
    <application
        android:name="${applicationName}"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">

        <!-- AdMob App‑ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4402835110551152~8514445452" />

        <!-- Registro automático del plugin de zona horaria (si lo usas) -->
        <meta-data
            android:name="com.example.undiamas.TzPlugin"
            android:value="flutterEmbedding" />

        <!-- ════════════════════════════════════════════════════════
             RECEPTORES necesarios para que las notificaciones
             programadas se entreguen en ‘release’ incluso con R8
             y con targetSdkVersion 34
        ════════════════════════════════════════════════════════  -->
        <!-- Receptor principal que lanza la notificación -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"
            android:permission="android.permission.SCHEDULE_EXACT_ALARM">
            <intent-filter>
                <action android:name="com.dexterous.flutterlocalnotifications.SCHEDULED_NOTIFICATION" />
            </intent-filter>
        </receiver>

        <!-- Restaura alarmas tras reinicio / actualización -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        <!-- ════════════════════════════════════════════════════════ -->

        <!-- -------- ACTIVIDAD PRINCIPAL -------- -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--  Versión de la embedding →
              necesario para que Flutter registre los plugins -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Permite que otras apps usen “Compartir → Procesar texto” -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
